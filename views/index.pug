extends layout

mixin standard-card(title, h1)
    div(class="standard-card")
        h2= title
        div(class="line")
            h1= h1
            if block
                block
            else
                p Nu har något gått fel! Sidan finns inte.

block content
    main.container-fluid
        div(class="row d-flex justify-content-center")
                +standard-card("Aktuellt","Välkommen till Bildterapihuset")
                    p Hej! Välkommen,


